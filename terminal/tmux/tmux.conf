set-option -g default-shell $SHELL

# settings for image preview to work in yazi
set -g allow-passthrough on

set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

set -sg escape-time 0

set -g prefix C-n
unbind C-b
bind-key C-n send-prefix

set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-256color:RGB"
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
set -as terminal-overrides ',*:Smulc=\E[4::%p1%dm'

set-option -g focus-events on

unbind %
bind | split-window -h

unbind '"'
bind - split-window -v

unbind r
bind r source-file $HOME/dotfiles/terminal/tmux/tmux.conf

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

bind -r m resize-pane -Z

set -g mouse on

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection

unbind -T copy-mode-vi MouseDragEnd1Pane


bind-key f split-window -v -c "#{pane_current_path}" 'yazi'

# list of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect' # Ctrl-s -- sasve session, Ctrl-r -- restore session

set -g status-position top
set -g window-status-separator ""
set -g status-left-length 50

set -g status-left "#[fg=1,bg=0]  #[fg=7,bg=0]#S "

set -g message-style "fg=0,bg=0"
set -g message-command-style "fg=0,bg=0"

set -g status-style "bg=0,fg=7"

set -g window-status-format "#[fg=7,bg=0] #I  #W "
set -g window-status-current-format "#[fg=6,bg=0] #I  #W "

set -g status-right-length 100
set -g status-right "#[fg=2,bg=0]  #[fg=7]#{pane_current_path} "

run '~/dotfiles/terminal/tmux/plugins/tpm/tpm'
